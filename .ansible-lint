profile: basic

exclude_paths:
  - roles/iptables/tests/

# shell is used intentionally in ipset_apply_one.yml to build dynamic
# ipset create commands with optional arguments.
warn_list:
  - command-instead-of-shell
